services:
  main:
    build: .
    container_name: jace
    environment:
      - DEEZER_ARL
      - DEEZER_KEY
      - DISCORD_APP_ID
      - DISCORD_BOT_TOKEN
      - ENV
      - MONGODB_COLLECTION_NAME
      - MONGODB_URL
      - SPOTIFY_CLIENT_ID
      - SPOTIFY_CLIENT_SECRET
    restart: always
