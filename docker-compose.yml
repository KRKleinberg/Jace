services:
  main:
    build: .
    container_name: jace
    environment:
      - DISCORD_APP_ID
      - DISCORD_BOT_TOKEN
      - ENV
      - MONGODB_URL
      - YOUTUBE_COOKIE
      - YOUTUBE_OAUTH
    restart: always
